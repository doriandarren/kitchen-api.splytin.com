import { response } from "express";

export const carbohydrateDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - carbohydrateDeleteController'
    });
}
