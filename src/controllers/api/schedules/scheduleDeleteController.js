import { response } from "express";

export const scheduleDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - scheduleDeleteController'
    });
}
