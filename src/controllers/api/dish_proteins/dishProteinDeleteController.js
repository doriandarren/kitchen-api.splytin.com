import { response } from "express";

export const dishProteinDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - dishProteinDeleteController'
    });
}
