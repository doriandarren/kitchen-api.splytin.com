import { response } from "express";

export const vegetableDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - vegetableDeleteController'
    });
}
