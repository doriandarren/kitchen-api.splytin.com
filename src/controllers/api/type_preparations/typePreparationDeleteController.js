import { response } from "express";

export const typePreparationDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - typePreparationDeleteController'
    });
}
