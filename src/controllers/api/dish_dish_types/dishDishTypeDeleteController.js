import { response } from "express";

export const dishDishTypeDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - dishDishTypeDeleteController'
    });
}
