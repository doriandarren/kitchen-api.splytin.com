import { response } from "express";

export const dishSeasonDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - dishSeasonDeleteController'
    });
}
