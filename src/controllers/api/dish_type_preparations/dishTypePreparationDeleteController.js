import { response } from "express";

export const dishTypePreparationDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - dishTypePreparationDeleteController'
    });
}
