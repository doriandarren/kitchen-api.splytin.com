import { response } from "express";

export const dishVegetableDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - dishVegetableDeleteController'
    });
}
