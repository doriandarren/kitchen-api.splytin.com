import { response } from "express";

export const dishTypeDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - dishTypeDeleteController'
    });
}
