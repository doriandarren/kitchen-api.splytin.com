import { response } from "express";

export const dishScheduleDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - dishScheduleDeleteController'
    });
}
