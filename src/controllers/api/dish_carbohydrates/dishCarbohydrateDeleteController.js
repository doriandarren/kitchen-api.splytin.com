import { response } from "express";

export const dishCarbohydrateDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - dishCarbohydrateDeleteController'
    });
}
